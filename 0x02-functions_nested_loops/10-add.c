#include "main.h"

/**
 * add - Function that adds two integer and returns the result
 * @a: First interger to be computed
 * @b: Second integer to be computed
 * Return: Always 0
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
